import QtQuick
// Carries the display enum the widgets reference as
// PlasmaComponents.AbstractButton.IconOnly.
QtObject {
    enum Display { IconOnly = 0, TextOnly = 1, TextBesideIcon = 2, TextUnderIcon = 3 }
}
